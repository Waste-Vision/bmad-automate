/* htmx stub — the base.html template loads htmx from this local path.
   For production, replace with the full htmx library from https://htmx.org/
   or load from CDN. This stub prevents 404 errors during development. */
console.log("htmx stub loaded — replace with full htmx.min.js for production use");
